// Demo page for Accordion
import Accordion from "../../components/ui-kit/Accordion.jsx";

export default function AccordionDemo() {
  return (
    <section>
      <h1>Accordion</h1>
      <p>
        A comprehensive, CSS-only accordion component with smooth animations and multiple variants. 
        Features include size options (small, regular, large), style variants (default, flush), 
        icon variations (chevron, plus/minus, caret), dark theme support, and enhanced content with 
        subtitles and badges. Perfect for FAQs, settings panels, navigation menus, or any collapsible 
        content structure. Fully accessible with keyboard navigation, focus management, and screen 
        reader support.
      </p>
      <Accordion />
    </section>
  );
}
