import Table from "../../components/ui-kit/Table.jsx";
export default function TablePage() {
  return (
    <section>
      <h1>Table</h1>
      <p>
        Comprehensive table system for displaying complex data with advanced features. 
        Includes multiple variants (striped, hover, bordered), size options, sortable columns, 
        row selection, responsive design, loading states, and pagination support. 
        Perfect for data-heavy applications with full accessibility compliance.
      </p>
      <Table />
    </section>
  );
}
