import Popover from "../../components/ui-kit/Popover.jsx";
export default function PopoverPage() {
  return (
    <section>
      <h1>Popover</h1>
      <p>
        A comprehensive CSS-only popover component for displaying rich interactive content.
        Features multiple positioning options (top, bottom, left, right), theme variants 
        (light, dark, error, success), size options, and support for complex content 
        including forms, menus, and user profiles. Perfect for tooltips, dropdown menus, 
        context menus, and interactive overlays with dismissible functionality.
      </p>
      <Popover />
    </section>
  );
}
