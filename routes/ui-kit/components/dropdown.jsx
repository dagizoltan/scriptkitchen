import Dropdown from "../../../components/ui-kit/Dropdown.jsx";
export default function DropdownPage() {
  return (
    <section>
      <h1>Dropdown</h1>
      <p>
        A CSS-only dropdown menu for navigation or actions. Useful for user menus, select lists, or any UI that requires a collapsible menu.
      </p>
      <Dropdown />
    </section>
  );
}
