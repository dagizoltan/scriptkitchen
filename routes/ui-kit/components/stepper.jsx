import Stepper from "../../../components/ui-kit/Stepper.jsx";
export default function StepperPage() {
  return (
    <section>
      <h1>Stepper</h1>
      <p>
        A CSS-only stepper component for displaying progress through a sequence of
        steps. Useful for multi-step forms, onboarding, or any process that can be
        broken down into stages.
      </p>
      <Stepper />
    </section>
  );
}
