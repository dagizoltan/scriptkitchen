// Demo page for MasonryGrid
import MasonryGrid from '../../../components/ui-kit/MasonryGrid.jsx';

export default function MasonryGridDemo() {
  return (
    <section>
      <h1>Masonry / Card Grid</h1>
      <p>
        A CSS-only masonry grid layout for displaying cards or items in a Pinterest-style, multi-column format. Useful for galleries, portfolios, or any content that benefits from a dynamic, gapless layout.
      </p>
      <MasonryGrid />
    </section>
  );
}
