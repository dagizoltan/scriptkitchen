import Timeline from "../../../components/ui-kit/Timeline.jsx";
export default function TimelinePage() {
  return (
    <section>
      <h1>Timeline</h1>
      <p>
        A CSS-only timeline component for displaying a series of events or steps in
        chronological order. Useful for histories, roadmaps, or process flows.
      </p>
      <Timeline />
    </section>
  );
}
