import Table from "../../../components/ui-kit/Table.jsx";
export default function TablePage() {
  return (
    <section>
      <h1>Table</h1>
      <p>
        A CSS-only table component for displaying tabular data. Includes styles for headers, rows, striping, and responsive layouts.
      </p>
      <Table />
    </section>
  );
}
