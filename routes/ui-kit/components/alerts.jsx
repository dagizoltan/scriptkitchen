import Alerts from "../../../components/ui-kit/Alerts.jsx";
export default function AlertsPage() {
  return (
    <section>
      <h1>Alerts</h1>
      <p>
        Provide contextual feedback messages for typical user actions with a
        handful of available and flexible alert messages. Includes styles for
        success, warning, error, and info states.
      </p>
      <Alerts />
    </section>
  );
}
