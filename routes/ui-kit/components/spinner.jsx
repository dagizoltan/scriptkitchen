import Spinner from "../../../components/ui-kit/Spinner.jsx";
export default function SpinnerPage() {
  return (
    <section>
      <h1>Spinner</h1>
      <p>
        A CSS-only loading spinner for indicating background activity or loading
        state. Useful for asynchronous actions, data fetching, or any process that
        takes time.
      </p>
      <Spinner />
    </section>
  );
}
