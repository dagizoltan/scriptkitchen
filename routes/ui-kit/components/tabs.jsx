// Demo page for Tabs
import Tabs from '../../../components/ui-kit/Tabs.jsx';

export default function TabsDemo() {
  return (
    <section>
      <h1>Tabs</h1>
      <p>
        A CSS-only tabbed navigation component for switching between different content panels. Useful for organizing content into logical sections without leaving the page.
      </p>
      <Tabs />
    </section>
  );
}
