// Demo page for TreeView
import TreeView from "../../components/ui-kit/TreeView.jsx";

export default function TreeViewDemo() {
  return (
    <section>
      <h1>Tree View</h1>
      <p>
        A CSS-only tree view component for displaying hierarchical data. Useful
        for file explorers, nested lists, or any data structure with
        parent-child relationships.
      </p>
      <TreeView />
    </section>
  );
}
